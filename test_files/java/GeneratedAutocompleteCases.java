public class GeneratedAutocompleteCases {
    static String formatStatus(String name, boolean active) {
        return name + ":" + (active ? "active" : "inactive");
    }

    static String caseCallCompletion(String user) {
        String status = formatStatus(
        return status;
    }

    static String caseSuffixOnly(String value) {
        return value.trim().toLowe
    }
}
