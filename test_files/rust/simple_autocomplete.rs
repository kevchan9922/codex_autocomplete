fn welcome(name: &str) -> String {
    format!("Welcome, {}!", name)
}

fn run_demo() {
    let name = "Mina";
    let message = welcome(
    println!("{}", message);
}

struct Profile {
    user_id: i32,
    user_name: String,
}

fn near_duplicate(profile: &Profile) -> i32 {
    profile.user_
}

fn vec_demo() -> Vec<i32> {
    let values = vec![1, 2, 3
    values
}

fn masked_word_demo() -> String {
    let name = "Mina";
    let message = welcome(name);
    messag
}

fn main() {
    run_demo();
}
