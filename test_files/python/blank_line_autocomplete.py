"""Blank-line Python autocomplete fixture."""


def build_message(name: str) -> str:
    return f"Hello, {name}!"


def blank_line_demo(user: dict[str, str]) -> str:
    normalized_name = user["name"].strip().title()
    # Assign message on the blank line below using build_message.
    
    return message
