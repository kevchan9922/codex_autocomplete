"""Pattern-focused Python autocomplete fixtures for bulk CLI benchmarking."""


def closure_paren_case(message: str) -> None:
    print(message


def closure_bracket_case() -> list[int]:
    numbers = [1, 2, 3
    return numbers


class UserProfile:
    def __init__(self, user_id: int, user_name: str):
        self.user_id = user_id
        self.user_name = user_name

    def pick_identifier(self) -> int:
        return self.use


def chain_lower_case(message: str) -> str:
    return message.strip().low
