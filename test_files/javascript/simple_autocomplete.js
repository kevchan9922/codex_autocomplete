function titleCase(input) {
  return input
    .split(" ")
    .filter(Boolean)
    .map((word) => word[0].toUpperCase() + word.slice(1).toLowerCase())
    .join(" ");
}

function welcome(name) {
  return `Welcome, ${name}!`;
}

function runDemo() {
  const normalized = titleCase("sAm lee");
  const message = welcome(
  console.log(message);
}

function optionalArgDemo(data) {
  const payload = JSON.stringify(data
  return payload;
}

function chainDemo(name) {
  const upper = name.to
  return upper;
}

function maskedWordDemo() {
  const normalized = titleCase("sAm lee");
  return normaliz;
}

runDemo();
