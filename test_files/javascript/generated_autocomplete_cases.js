function formatStatus(name, active) {
  return `${name}:${active ? "active" : "inactive"}`;
}

function caseCallCompletion(user) {
  const status = formatStatus(
  return status;
}

function caseSuffixOnly(value) {
  return value.trim().toLowe
}
